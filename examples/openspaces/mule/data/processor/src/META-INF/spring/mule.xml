<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesource.org/schema/mule/core/2.0"
      xmlns:os-events="http://www.openspaces.org/schema/events"
      xmlns:os-core="http://www.openspaces.org/schema/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:spring="http://www.springframework.org/schema/beans"
      xmlns:os-eventcontainer="http://www.openspaces.org/schema/mule/os-eventcontainer"      xmlns:stdio="http://www.mulesource.org/schema/mule/stdio/2.0"
      xsi:schemaLocation="http://www.openspaces.org/schema/core http://www.openspaces.org/schema/core/openspaces-core.xsd
       http://www.openspaces.org/schema/events http://www.openspaces.org/schema/events/openspaces-events.xsd
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
       http://www.mulesource.org/schema/mule/core/2.0 http://www.mulesource.org/schema/mule/core/2.0/mule.xsd
       http://www.openspaces.org/schema/mule/os-eventcontainer http://www.openspaces.org/schema/mule/os-eventcontainer.xsd
       http://www.mulesource.org/schema/mule/stdio/2.0 http://www.mulesource.org/schema/mule/stdio/2.0/mule-stdio.xsd">

    <spring:beans>
        <!--
            A bean representing a space (an IJSpace implementation).

            Note, we do not specify here the cluster topology of the space. It is declated outside of
            the processing unit or within the SLA bean.
        -->
        <os-core:space id="space" url="/./space"/>

        <!--
            OpenSpaces simplified space API built on top of IJSpace/JavaSpace.
        -->
        <os-core:giga-space id="gigaSpace" space="space"/>

        <!--
            A polling event container that perfoms (by default) polling take operations against
            the space using the provided template (in our case, and the non processed data objects).
            Once a match is found, the data processor bean event listener is triggered using the
            annotation adapter.
        -->
        <os-events:polling-container id="dataProcessorPollingEventContainer" giga-space="gigaSpace">
            <os-core:template>
                <spring:bean class="org.openspaces.example.mule.data.common.Data">
                    <spring:property name="processed" value="false"/>
                </spring:bean>
            </os-core:template>
        </os-events:polling-container>

        <!--
            A notified event container counting all the processed data objects using notifications basde on
            an sql template.
        -->
        <os-events:notify-container id="dataProcessedCounterNotifyContainer" giga-space="gigaSpace" com-type="UNICAST"
                                    perform-take-on-notify="false" ignore-event-on-null-take="true">
            <os-events:notify write="true" update="true"/>
            <os-events:batch size="10" time="5000"/>
            <os-core:sql-query where="processed = true" class="org.openspaces.example.mule.data.common.Data"/>
        </os-events:notify-container>

    </spring:beans>


    <os-eventcontainer:connector name="gigaSpacesConnector"/>

    <model name="processor">
        <service name="DataProcessorUMO">
            <inbound>
                <inbound-endpoint address="os-eventcontainer://dataProcessorPollingEventContainer"/>
            </inbound>

            <component class="org.openspaces.example.mule.data.processor.DataProcessor"/>
            
            <outbound>
                <outbound-pass-through-router>
                    <outbound-endpoint address="os-eventcontainer://gigaSpace"/>
                </outbound-pass-through-router>
            </outbound>
        </service>

        <service name="DataProcessedCounterUMO">
            <inbound>
                <inbound-endpoint address="os-eventcontainer://dataProcessedCounterNotifyContainer"/>
            </inbound>

            <component class="org.openspaces.example.mule.data.processor.DataProcessedCounter"/>
            
            <!--
                An outbound router can have one or more router configurations that can be
                invoked depending on business rules, message contents, headers or any other
                criteria. The pass-through-router is a router that automically passes
                on every message it receives
            -->
            <outbound>
                <outbound-pass-through-router>
                    <outbound-endpoint address="stdio://System.out"/>
                </outbound-pass-through-router>
            </outbound>
        </service>

    </model>
</mule>